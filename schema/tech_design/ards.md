# Architecture Decisions: {topic} (架构决策 + 方案对比)

> **状态**: Draft | Ready | Implemented | Deprecated
> **来源 PRD**: [../../prd/{YYYY-MM-DD}/{topic}.md](../../prd/{YYYY-MM-DD}/{topic}.md)
> **创建日期**: {YYYY-MM-DD}
> **最后更新**: {YYYY-MM-DD}

> **v4 必填子文件**: 架构决策的**唯一落点**——方向选择 + 方案对比 (Solution Space) + 关键决策 (KD) + 探索记录 (OQ/Spikes/Pivot) + 选定 vs 实际代码 Gap 对比 + 补全建议 + 风险.
> 回答两个问题: 「为什么选这个方案」(对比与理由) 和 「落地后与现状差多少」(Gap 与补全).

---

## 探索深度声明 (Exploration Depth)

> 每个 TD 必须显式回答"为什么选这个方案", **不是**直接给方向. 按实现状态选择深度:

| TD 状态 | 深度 | 要求 |
|----------|------|------|
| **未实现** (无任何代码) | 深度 | Solution Space ≥ 3 候选方案 + Spikes ≥ 3 + Pivot Triggers ≥ 3 |
| **部分实现 + trade-off** | 中度 | Solution Space ≥ 2 候选方案 + trade-off 缓解 + Spikes + Pivot |
| **已实现 (核心完整)** | 轻度 | Solution Space ≥ 2 候选方案 (回填) + 未来 TODO + Pivot |
| **实现完整 + 无 TODO** | N/A | 1 段说明"已实现, 无探索", 其余段落可省略 |

**本 TD 声明**: [未实现 / 部分实现 / 已实现 / 实现完整] → 按 [深度 / 中度 / 轻度 / N/A] 填写

---

## 方向选择与方案对比 (Direction & Solution Space)

### 方向选择总表

> 每个决策点对应下方一个 KD. 每个 KD 至少 2 个备选方案.

| 决策维度 | 选定方向 | 替代方向 | 选择依据 | KD |
|---------|---------|---------|---------|---|
| [维度 1] | [选定] | [替代 1, 替代 2] | [依据] | KD-1 |
| [维度 2] | [选定] | [替代 1, 替代 2] | [依据] | KD-2 |

### 方案对比矩阵

| 方案 | 实现路径 | 优点 | 缺点 | 成本 | 评分 (满分 5) |
|------|---------|------|------|------|----------------|
| **A: [名称]** | [路径] | [优点] | [缺点] | [Xd] | 综合 ⭐ |
| **B: [名称]** | [路径] | [优点] | [缺点] | [Xd] | 综合 ⭐ |
| **C: [名称]** | [路径] | [优点] | [缺点] | [Xd] | 综合 ⭐ |

### 方案详细对比

#### 方案 A: [名称]

**实现路径**:
- [具体实施步骤 1]
- [具体实施步骤 2]

**优点**:
- [优点 1]

**缺点**:
- [缺点 1]

**何时选**: [条件]

#### 方案 B: [名称]

[同上结构]

---

## 探索记录 (Open Questions & Spikes)

> 「未实现/部分实现」深度必填; 「已实现」轻度填 TODO; N/A 可省略.

### Open Questions (待 Spike 回答)

> 「既有/现状即契约」的依赖, 前提未验证的一律进本表.

| # | 问题 | 重要性 | 解决方式 | 当前假设 |
|---|------|--------|----------|----------|
| Q1 | [问题] | 🔴 关键 / 🟡 重要 / 🟢 后续 | [Spike / 调研 / 决策] | [假设] |

### Spikes (技术验证计划)

| # | Spike | 时间 | 目标 | 验收标准 | 依赖 |
|---|-------|------|------|----------|------|
| **SP-1** | [名称] | [时间] | [目标] | [验收] | [依赖] |

**总时长**: [估算]

### Pivot Triggers (何时重新评估)

| 触发 | 检测信号 | 切换方向 |
|------|----------|----------|
| **P1**: [触发条件] | [如何检测] | [A → B] |
| **P2**: [触发条件] | [如何检测] | [A → C] |

---

## KD-1: {决策名}

> **锚点格式固定**: `## KD-{n}: {名称}` (h2), 供 Plan / 校验脚本跨文件引用.

**背景 (Context)**: 什么情况下需要做这个决策？涉及哪些模块？

**决定 (Decision)**: 最终选择是什么？（一句话）

**备选方案 (Alternatives)**:

| 选项 | 优点 | 缺点 |
|------|------|------|
| {选项 A} | ... | ... |
| {选项 B} | ... | ... |

**理由 (Rationale)**: 为什么选这个而不是其他？

**如何强制 (Enforced by)**: code review / lint / 约定 / 测试

**关联 (References)**:
- 时序图: SYS-{n} / MOD-{n} (见 [./apis.md](./apis.md))
- PRD 场景: {n}
- 验证用例: V-{n}

---

## KD-2: ...

---

## 现状对比与补全 (Selected vs Actual)

> 对比 TD 选定设计与当前代码实际状态, 列出 Gap. 实际代码状态需在编写时进行摸底, 不可凭猜测.

### Gap 表

| 选定方向 | 实际代码 | Gap | 状态 |
|---------|---------|-----|------|
| [选定模块/端点] | ✅ 已就位 / ⚠️ 部分 / ❌ 不存在 | [描述] | ✅ / 🟡 / ❌ |

### 综述

| 维度 | 已实现 | 部分实现 | 待建 | 备注 |
|------|--------|---------|------|------|
| [模块 1] | [%] | [%] | [%] | [备注] |
| **全链路** | **[%]** | ... | **[%]** | **[总结]** |

### 补全建议 (按优先级)

| Gap | 优先级 | 工作量 | 建议实施顺序 |
|-----|--------|--------|-------------|
| [Gap 描述] | P0 / P1 / P2 | [Xd / Yh] | Phase N |

---

## 风险与缓解 (Risks & Mitigations)

| 风险 | 触发条件 | 影响 | 缓解 |
|------|---------|------|------|
| [风险 1] | [触发] | [影响] | [缓解策略 + Pivot Trigger 编号] |

---

## 门禁清单 (Completion Gate)

- [ ] 探索深度声明已填写, 与 TD 实现状态匹配
- [ ] Solution Space 候选方案数 ≥ 深度要求, 每个方案有 优/缺/成本 评估
- [ ] 方向选择总表每个决策点对应一个 KD
- [ ] 每个 KD 有完整 5 段 (Context / Decision / Alternatives / Rationale / Enforced by), ≥ 2 备选方案
- [ ] KD 锚点格式为 `## KD-{n}: {名称}` (h2)
- [ ] Spikes 有验证计划 (时间 + 目标 + 验收标准); Pivot Triggers ≥ 3 (深度/中度)
- [ ] Gap 对比表覆盖选定模块, 综述百分比与明细一致
- [ ] 补全建议按优先级排序, P0 项与 Plan 衔接
- [ ] 每个风险有缓解策略, 并关联 Pivot Trigger

---

## 变更记录 (Change Log)

| 日期 | 变更 |
|------|------|
| {YYYY-MM-DD} | 初始: ards.md (v4 结构, 合并 v3 的 decisions + exploration) |