# Scenario Mapping: {topic}

> **状态**: Draft | Ready
> **来源 PRD**: [../../prd/{YYYY-MM-DD}/{topic}.md](../../prd/{YYYY-MM-DD}/{topic}.md)
> **创建日期**: {YYYY-MM-DD}
> **最后更新**: {YYYY-MM-DD}

> ⚠️ **完整性原则**: PRD 中每个 Given/When/Then 场景**必须**出现在本表中,漏掉会导致 Plan 验证用例不完整。

---

## 场景映射表

| PRD 场景 (或 FR-XXX) | 步骤 | 动作 | 组件 | 数据模型 | 时序图 |
|----------------------|------|------|------|----------|--------|
| {场景 A 或 FR-001} | 1 | {动作} | {组件} | {模型} | MOD-1 |
|                      | 2 | {动作} | {组件} | {模型} | MOD-1 |
| {场景 B 或 FR-002} | 1 | ... | ... | ... | SYS-1 |

**字段说明**:
- **PRD 场景 (或 FR-XXX)**: 来自 PRD 的场景编号,如果 PRD 用了 FR-XXX 编号则用 FR-XXX
- **步骤**: 场景内的步骤序号(从 1 开始)
- **动作**: 该步骤执行的动作
- **组件**: 负责该动作的内部组件
- **数据模型**: 该动作涉及的数据实体
- **时序图**: 对应的时序图编号

---

## 完整性核对

- [ ] PRD 场景 1 → 已在表中 → SYS/MOD 引用正确
- [ ] PRD 场景 2 → 已在表中 → SYS/MOD 引用正确
- [ ] PRD 异常场景 1 → 已在表中 → SYS/MOD 引用正确
- [ ] PRD 边界场景 1 → 已在表中 → SYS/MOD 引用正确

---

## 门禁清单 (Completion Gate)

- [ ] PRD 中每个 Given/When/Then 场景都在表中
- [ ] 异常和边界场景不遗漏
- [ ] 表格每行都有组件 + 数据模型 + 时序图(如果时序图存在)
- [ ] 完整性核对 4 项已勾选

---

## 变更记录 (Change Log)

| 日期 | 变更 |
|------|------|
| {YYYY-MM-DD} | 初始 |
