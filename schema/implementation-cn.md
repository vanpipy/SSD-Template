# Skill: [技能名称]

> **视角：执行 Agent**
> Implementation 是写给执行 Agent 用的。Agent 将逐条执行变更清单中的每一项，不需要猜测或补充。
> - 变更清单的每个结构要求必须包含函数/类型签名（代码块），不接受纯自然语言描述。
> - 验证用例必须关联到具体的变更项和函数，Agent 执行完毕后按此验证。
> - 仓库上下文必须在设计阶段扫描填充完毕，Agent 执行时不应再有任何"待确认"信息。
> - 完成清单是 Agent 的交付标准，所有检查项必须通过。

## 标识
- 名称: [技能名称]
- 描述: [一句话描述这个技能做什么]
- 版本: [语义版本号]
- 状态: [草稿 | 就绪 | 废弃]
- 来源: [指向 tech-design 目录下对应 Tech Design 文件的路径]

## 仓库上下文
- Agent 指令来源: [Qoder 三级 / CLAUDE.md / .cursorrules / 无 — 列出找到的文件路径]
  - Qoder 用户级: [~/.qoder/AGENTS.md 是否存在，关键内容摘要]
  - Qoder 项目级: [AGENTS.md 是否存在，关键内容摘要]
  - Qoder 本地级: [AGENTS.local.md 是否存在，关键内容摘要]
  - 其他工具: [CLAUDE.md / .cursorrules 等，如无可省略]
  - 构建命令: [从 Agent 指令文件提取，如 `pnpm build`]
  - 测试命令: [从 Agent 指令文件提取，如 `pnpm test`]
  - Lint 命令: [从 Agent 指令文件提取，如 `pnpm lint --fix`]
  - 关键约定: [从 Agent 指令文件提取的核心规则摘要，如"函数式组件"、"co-locate tests"]
  - 冲突记录: [与变更清单的矛盾之处及处理决策，无则写"无"]
- 技术栈: [从扫描清单第1步识别]
- 代码风格: [命名规范、注释风格、目录组织，从第2、7步得出]
- 现有接口: [路由定义位置、类型定义位置、API 规范位置，从第3、4步得出]
- 参考实现: [第5步找到的相似模块路径]
- 测试约定: [测试目录、框架、写法参考，从第6步得出]
- 冲突检查: [变更清单中的文件是否与现有代码冲突？如有，说明处理方式]

## 适用范围
- 适用: [什么场景下应该使用本技能]
- 不适用: [什么场景下不应使用本技能，容易误用的场景特别标注]

## 接口
- 输入:
  - [参数名] ([类型], [必填 | 选填]) - [含义]，示例: [示例值]
- 成功输出: [描述成功时的返回结构]
- 失败输出: [描述失败时的返回结构]
- 错误码:
  - [错误码]: [错误含义]
  - [错误码]: [错误含义]

## 业务约束
- 前置条件:
  1. [调用前必须满足的条件]
  2. [调用前必须满足的条件]
- 不变式: [执行过程中始终成立的约束]
- 后置条件:
  1. [调用成功后必须满足的条件]
  2. [调用成功后必须满足的条件]
- 副作用:
  1. [调用后会改变的外部状态或触发的操作]
  2. [调用失败时是否产生副作用，明确说明]

## 依赖
- 前置Skill: [需要先调用的其他技能，如无则写"无"]
- 后置Skill: [执行成功后需要调用的其他技能，如无则写"无"]
- 外部系统: [依赖的外部接口或服务，及其超时配置]
- 协作模式: [顺序调用 | 并行调用 | 责任链 | 发布订阅 | 其他]

## 开发约束
- 并发策略: [如何处理并发调用，使用的锁机制或冲突检测方式]
- 事务边界: [哪些操作必须在同一事务中，哪些明确不在事务范围内]
- 幂等性: [如何保证幂等，去重的唯一键是什么，重复请求的行为]
- 重试与降级:
  1. [什么情况触发重试，重试次数和间隔]
  2. [重试失败后如何处理，降级策略或补偿机制]
  3. [异步回调和同步等待的边界]
- 安全要求:
  1. [权限校验规则]
  2. [数据脱敏要求]
  3. [审计日志要求]
- 外部系统约束:
  1. [调用外部系统时的超时设置]
  2. [外部系统回调或异步通知的处理方式]

## 实现策略
- 测试先行: [是 | 否] — 是否要求先根据"验证"块生成测试用例，再编写实现代码
- 行为驱动: [是 | 否] — 是否要求用 Given/When/Then 格式描述核心场景后再实现

## 变更清单

> 按序号顺序执行。每个变更项必须是可独立完成的最小单元。
> **结构要求**字段必须列出要定义的函数/类/类型签名，不允许只写自然语言描述。
> 如果某项签名尚未确定，在待决策中标注，不得自行发明。

### 新建

#### 变更 #1: [文件路径]
- 执行序号: [1, 2, 3, ... — 决定实现顺序]
- 依赖: [无 | 依赖变更 #N — 说明为什么]
- 结构要求:
  ```
  // 必须定义的接口/类型/函数签名（伪代码或目标语言）
  interface XxxRequest {
    fieldA: string    // 含义说明
    fieldB: number    // 含义说明
  }

  function doXxx(request: XxxRequest): XxxResponse {
    // 职责：一句话描述这个函数做什么
    // 不做：一句话描述明确排除的行为
  }
  ```
- 业务规则映射: [对应「业务约束」中的哪些前置/后置条件或不变式]

#### 变更 #2: [文件路径]
- 执行序号: [...]
- 依赖: [...]
- 结构要求:
  ```
  ...
  ```
- 业务规则映射: [...]

### 修改

#### 变更 #N: [文件路径]
- 执行序号: [...]
- 依赖: [无 | 依赖变更 #M]
- 修改位置: [函数名/类名/代码行范围]
- 修改内容:
  ```
  // 修改前（关键片段）
  function existingFn(...) { ... }

  // 修改后（关键片段）
  function existingFn(...newParam: Type) { ...新增逻辑... }
  ```
- 影响范围: [这次修改会影响哪些调用方？需不需要同步改？]
- 业务规则映射: [...]

### 删除

#### 变更 #N: [文件路径]
- 执行序号: [...]
- 删除原因: [...]
- 引用清理: [删除后需要清理哪些对该文件的引用？]

## 验证

> 每条验证用例必须关联到具体的变更项和函数，确保可追溯到实现代码。

### V1: [场景名称]
- 关联变更: [变更 #N]
- 关联函数: [函数名]
- Given: [前置条件]
- When: [操作]
- Then: [预期结果]

### V2: [场景名称]
- 关联变更: [变更 #N]
- 关联函数: [函数名]
- Given: [前置条件]
- When: [操作]
- Then: [预期结果]

### V3: [边界/异常场景]
- 关联变更: [变更 #N]
- 关联函数: [函数名]
- Given: [前置条件 — 触发边界或异常的条件]
- When: [操作]
- Then: [预期结果 — 错误码、降级行为等]

---

## 完成清单

> 执行完毕后逐项勾选。全部完成才算 Done。
> 命令来源：仓库上下文中记录的构建/测试/Lint 命令。

- [ ] **代码实现** — 变更清单中所有变更项已按序号顺序完成
- [ ] **类型检查** — 使用仓库上下文中的类型检查命令，无错误
- [ ] **Lint 通过** — 使用仓库上下文中的 Lint 命令，无错误
- [ ] **测试通过** — 验证章节中所有用例已转化为测试并跑通
- [ ] **无遗留 TODO** — 代码中没有未解决的 TODO 或 FIXME
- [ ] **PR 已创建** — 按版本控制章节要求创建 PR（如适用）

## 变更记录
- [版本号] / [日期] / [变更内容和原因]

