# Skill: [技能名称]

## 标识
- 名称: [技能名称]
- 描述: [一句话描述这个技能做什么]
- 版本: [语义版本号]
- 状态: [草稿 | 就绪 | 废弃]
- 来源: [指向 collection 目录下对应采集条目的路径，如无可省略]

## 版本控制
- 仓库: [Git 仓库地址或本地路径，如 https://github.com/org/repo 或 ./src]
- 分支命名规范:
  - 功能分支: [功能分支命名模式，如 feature/{ticket-id}-{简短描述}]
  - 修复分支: [修复分支命名模式，如 bugfix/{ticket-id}-{简短描述}]
  - 发布分支: [发布分支命名模式，如 release/v{版本号}]
- Pull Request 流程:
  1. [步骤1：如，从 main 分支创建功能分支]
  2. [步骤2：如，使用原子化提交进行实现]
  3. [步骤3：如，确保本地所有测试通过]
  4. [步骤4：如，推送分支并创建 Pull Request]
  5. [步骤5：如，向指定审核人请求评审]
  6. [步骤6：如，处理反馈并压缩提交]
  7. [步骤7：如，审批通过后合并]
- PR 要求:
  - 必要审批数: [必须通过的审批数量]
  - 必要检查项: [合并前必须通过的 CI 检查，如测试、lint、构建]

## 仓库上下文 (执行前由 agent 填写)
> ⚠️ 上方的"仓库"字段仅指定目标地址。执行前需先 clone 仓库并扫描代码结构，填写以下上下文。
- 技术栈: [从 package.json、go.mod、requirements.txt 等识别]
- 代码风格: [命名规范、注释风格、目录组织]
- 现有接口: [路由定义位置、类型定义位置、API 规范位置]
- 冲突检查: [变更清单中的文件是否与现有代码冲突？如有，说明处理方式]

## 适用范围
- 适用: [什么场景下应该使用本技能]
- 不适用: [什么场景下不应使用本技能，容易误用的场景特别标注]

## 接口
- 输入:
  - [参数名] ([类型], [必填 | 选填]) - [含义]，示例: [示例值]
- 成功输出: [描述成功时的返回结构]
- 失败输出: [描述失败时的返回结构]
- 错误码:
  - [错误码]: [错误含义]
  - [错误码]: [错误含义]

## 业务约束
- 前置条件:
  1. [调用前必须满足的条件]
  2. [调用前必须满足的条件]
- 不变式: [执行过程中始终成立的约束]
- 后置条件:
  1. [调用成功后必须满足的条件]
  2. [调用成功后必须满足的条件]
- 副作用:
  1. [调用后会改变的外部状态或触发的操作]
  2. [调用失败时是否产生副作用，明确说明]

## 依赖
- 前置Skill: [需要先调用的其他技能，如无则写"无"]
- 后置Skill: [执行成功后需要调用的其他技能，如无则写"无"]
- 外部系统: [依赖的外部接口或服务，及其超时配置]
- 协作模式: [顺序调用 | 并行调用 | 责任链 | 发布订阅 | 其他]

## 开发约束
- 并发策略: [如何处理并发调用，使用的锁机制或冲突检测方式]
- 事务边界: [哪些操作必须在同一事务中，哪些明确不在事务范围内]
- 幂等性: [如何保证幂等，去重的唯一键是什么，重复请求的行为]
- 重试与降级:
  1. [什么情况触发重试，重试次数和间隔]
  2. [重试失败后如何处理，降级策略或补偿机制]
  3. [异步回调和同步等待的边界]
- 安全要求:
  1. [权限校验规则]
  2. [数据脱敏要求]
  3. [审计日志要求]
- 外部系统约束:
  1. [调用外部系统时的超时设置]
  2. [外部系统回调或异步通知的处理方式]

## 实现策略
- 测试先行: [是 | 否] — 是否要求先根据"验证"块生成测试用例，再编写实现代码
- 行为驱动: [是 | 否] — 是否要求用 Given/When/Then 格式描述核心场景后再实现

## 变更清单
### 新建
- [文件路径]: [新文件内容说明]
- [文件路径]: [新文件内容说明]

### 修改
- [文件路径]: [修改内容说明]
- [文件路径]: [修改内容说明]

### 删除
- [文件路径]: [删除原因]
- [文件路径]: [删除原因]

## 验证
- [场景描述]: 预期 [预期结果]
- [场景描述]: 预期 [预期结果]
- [场景描述]: 预期 [预期结果]
- [场景描述]: 预期 [预期结果]

## 变更记录
- [版本号] / [日期] / [变更内容和原因]
