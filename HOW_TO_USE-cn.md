# Skill-Driven 使用指南

## 这个指南解决什么问题

你有一个需求，可能是用户的一句抱怨、产品的一句话、一段模糊的讨论，或者一场需求会议的完整纪要。你需要把它们变成 AI 可以直接编译成代码的实施计划。这个指南告诉你一步步怎么做。

## 目录组织规范

每次输入创建一个**批次子文件夹**，统一收纳该批次的所有产出：

```
collection/
├── {日期}-{简短主题}/           # 批次文件夹
│   ├── C01-xxx.md               # Collection 文件
│   ├── C02-xxx.md               # Collection 文件
│   └── BATCH-OVERVIEW.md        # 批次概览（Agent 拆分工作的产物）

tech-design/
├── {日期}-{简短主题}/           # 批次文件夹
│   └── td-01-xxx.md             # Tech Design 文件

implementation/
├── {日期}-{简短主题}/           # 批次文件夹
│   ├── impl-01-xxx.md           # Implementation 文件
│   └── code/                    # 生成的代码（可选）
```

- **批次文件夹命名**：`{YYYY-MM-DD}-{简短主题}`
- **每个批次**包含：本次输入产生的所有文件 + 批次概览
- **批次概览**：Agent 拆分工作的产物，下游 Agent 会把它当作全局上下文来理解 Collection 间的关系

## 使用场景

统一入口：无论输入是单个需求、一份会议摘要、还是介于两者之间的任何内容，都走第一至六步。

## 操作流程

> **视角说明**
> 本流程采用**双角色协作模型**：
> - **Agent（起草者 / 调查者 / 产出者）**：从输入中提取结构，起草产物，对不确定的内容标注 `⚠`，在下游产出技术文档。
> - **参与人（审阅者 / 决策者）**：对照实际讨论内容验证草稿，解决 `⚠` 标记，对模糊点做最终决策。
>
> 每次交接都遵循同一节奏：**Agent 提议 → 参与人决策 → Agent 落盘**。
>
> 流程分为两个阶段：
> - **需求阶段（第一步 ~ 第二步）**：Collection 阶段。Agent 从输入起草；参与人审阅；收敛讨论直到所有规则可验证。状态流转：`采集中 → 已收敛`。
> - **技术阶段（第三步 ~ 第五步）**：Tech Design + Implementation 阶段。Agent 基于已收敛的 Collection 产出技术文档。参与人审核正确性；Agent 执行。状态流转：`草稿 → 就绪 → 已执行`。

### 一、第一步 - 采集需求

> Agent 起草 Collection；参与人审阅

输入是任意形态的需求材料——单个需求描述、会议摘要、产品的一句话、讨论记录，或介于其间的任何内容。Agent 自动判断输入产出单个还是多个 Collection，按拆分/合并原则处理。输出是一个或多个 Collection 文件，放在 `collection/{日期}-{简短主题}/` 目录下，状态为**采集中**。

操作方法：
1. 根据输入来源确定批次文件夹名称
2. 使用提示词 [提示词一：采集需求]
3. **审阅交接**：Agent 呈现起草的 Collection，对不确定的字段标注 `⚠`。如果输入产出多个 Collection，Agent 先呈现批次概览（清单 + 关系图），再呈现各个 Collection。你逐条处理 `⚠` 标记（接受 / 修正 / 补充模糊点）。

**轻量模式**：当 Agent 识别出的独立需求点 ≤ 4 个且无跨角色/跨系统耦合时，可能提议轻量模式。此时批次概览简化，Collection 可合并。

### 二、第二步 - 收敛讨论

> Agent 提议收敛建议；参与人决策；Agent 落盘

输入是采集中状态的 Collection 文件（位于批次文件夹内）。输出是状态变为已收敛的同一文件，所有模糊点清零。

操作方法：使用提示词 [提示词二：收敛讨论]

**协作节奏**：Agent 读取 Collection（多 Collection 时同时读取 BATCH-OVERVIEW），产出一份**收敛建议报告**，逐条列出每个模糊点的 AI 建议解，并标注"需要你决策"的部分。你逐条决策；Agent 把决策写入 Collection 并同步批次概览。

如果是从第一步拆分出来的多个 Collection，注意维护它们之间的关系。一个 Collection 收敛过程中如果影响到另一个 Collection，Agent 需要同步更新批次级别的 BATCH-OVERVIEW.md（具体为跨 Collection 关系图与全局待收敛清单）。

### 三、第三步 - 生成技术设计

> 执行 Agent 视角

输入是状态为已收敛的 Collection 文件，以及目标仓库的技术上下文。输出是一个 Tech Design 文件，放在 `tech-design/{日期}-{简短主题}/` 目录下，状态为草稿。

技术设计是 Collection（业务规则）和 Implementation（代码指令）之间的桥梁。它围绕四个核心关注点展开：**技术策略**（整体怎么建）、**指标**（衡量标准是什么）、**方案全量调查**（端到端过程是否都审过一遍）、**需求转换兜底**（业务意图是否在转换中丢失）。流程按**设计模式**（全新设计 / 存量设计）分支，存量设计是默认模式。

操作方法：
1. 在 `tech-design/` 下创建与 collection 同名的批次文件夹
2. 使用提示词 [提示词三：生成技术设计]

审核要点：技术策略是否明确了整体路线和关键决策、指标是否给出了具体数字而非模糊描述、方案全量调查是否覆盖了端到端流程/组件交互/数据流三个维度且粒度可审计、需求转换兜底策略的覆盖矩阵是否逐条核对了 Collection 场景、降级方案是否说明了业务影响、四支柱之间是否通过了跨切面一致性审查且无内在冲突。审核通过后，把状态从草稿改为就绪，并将 Tech Design 文件路径回填到 Collection 的关联技术设计字段。

### 四、第四步 - 生成实施计划

> 执行 Agent 视角

输入是状态为就绪的 Tech Design 文件。输出是一个 Implementation 文件，放在 `implementation/{日期}-{简短主题}/` 目录下，状态为草稿。

操作方法：
1. 在 `implementation/` 下创建与 collection 同名的批次文件夹
2. 使用提示词 [提示词四：生成实施计划]

审核要点：AI 生成初稿后，检查变更清单的结构要求是否与 Tech Design 的 API 契约和数据模型一致、验证用例是否覆盖核心路径。审核通过后，把状态从草稿改为就绪，同时把对应 Collection 的状态从已收敛改为已完成，并将 Implementation 文件路径回填到 Collection 的关联实现字段。

### 五、第五步 - 执行实施计划

> 执行 Agent 视角

输入是状态为就绪的 Implementation 文件，可附带当前项目上下文，如代码库路径、技术栈说明。

**触发指令：**
```
执行 implementation {implementation-file-path}
```

Agent 将读取 Implementation 文件，按照其中的指导执行。

### 补充流程：修改已有实施计划

1. 如果一个状态为草稿的 Implementation 需要修改，直接编辑即可。
2. 如果一个状态为就绪或已完成的 Implementation 需要修改，不要直接编辑它。回到第一步，新建一个 Collection。在关联实现字段里填上要修改的 Implementation 路径，主题写清楚要改什么。收敛完毕后，经技术设计、生成实施计划，审核通过后新版本状态变为就绪，旧版本保留在变更记录里。
3. 如果只是技术设计需要调整（不涉及业务规则变更），可以直接新建 Tech Design 版本，重新生成 Implementation。

---

## 输入输出速查

第一步采集需求。输入是任意形态的需求材料（单个需求、会议摘要、或介于两者之间），输出是一个或多个状态为采集中的 Collection 文件，多 Collection 时附带批次概览（位于批次文件夹）。
第二步收敛讨论。输入是采集中的 Collection 文件，输出是状态为已收敛的同一文件。
第三步生成技术设计。输入是已收敛的 Collection 文件，输出是状态为草稿的 Tech Design 文件（位于批次文件夹）。
第四步生成实施计划。输入是就绪状态的 Tech Design 文件，输出是状态为草稿的 Implementation 文件（位于批次文件夹）。审核通过后状态变为就绪。
第五步执行。输入是状态为就绪的 Implementation 文件，输出是实现代码。

相关文件

- Collection 模板：schema/collection-cn.md
- 批次概览模板：schema/batch-overview-cn.md
- Tech Design 模板：schema/tech-design-cn.md
- Implementation 模板：schema/implementation-cn.md

---

## 提示词一：采集需求

输入：任意形态的需求材料——单个需求描述、会议摘要、产品的一句话、讨论记录，或介于其间的任何内容。可以是粘贴的全文或本地文件路径。
输出：一个或多个 Collection 文件，放在批次文件夹 `collection/{日期}-{简短主题}/` 下，状态为采集中。如果输入产出多个 Collection，同时产出 `BATCH-OVERVIEW.md`。

提示词内容：

任务：将需求材料转化为 Collection 文件，按拆分/合并原则决定输出单个还是多个 Collection。你是起草者，参与人是审阅者与决策者。对不确定的内容标注 `⚠`，让参与人知道哪里需要他做判断。不得跳过审阅节点。

步骤：

1. **自动识别输入类型**。阅读输入，判断：
   - **单需求输入**：一个明确的需求，无独立子需求 → 产出 1 个 Collection。跳到步骤 3。
   - **多需求输入**：会议摘要、讨论记录、或包含多个独立需求的任何输入 → 产出多个 Collection + 批次概览。继续步骤 2。
2. **拆分决策**（仅多需求输入）。列出输入中提到的所有需求点，再按下述原则决定拆分颗粒度：
   - **拆**：业务能力边界清晰（登录 vs 结算 vs 退货）、生命周期独立（创建/修改/归档不在同一流程）、由不同角色操作（店员 vs 顾客 vs 管理员）。
   - **合**：前后连贯的端到端流（加购 → 支付 → 小票）、共享大部分数据模型和外部系统、讨论中始终被一起提及、单独拆出后内容很少（少于 2 条规则）。
   - **宁可少拆不要多拆**：合并的 Collection 可以用多个场景小节承载，拆开后跨 Collection 的管理成本高于阅读成本。
   - **软上限**：如果拆分结果超过 8 个 Collection，必须对第 8 个之后的每一条给出显式理由。
   - **轻量模式判定**：如果独立需求点 ≤ 4 个且不存在跨角色/跨系统耦合，提议轻量模式（在批次概览顶部说明理由）。轻量模式下批次概览简化，Collection 可进一步合并。
   - 多需求时，先向参与人呈现提议的拆分数量与理由、Collection 清单表、跨 Collection 关系图、以及"我不确定的拆分决策"清单。等待参与人 (a) 接受、(b) 要求合并/拆分、(c) 要求按轻量模式重新拆分。按反馈调整后再继续。
3. **起草每个 Collection**。对每个需求点（单需求或拆分后的多需求），按 `schema/collection-cn.md` 格式生成一个 Collection 文件。状态设为采集中。对不确定的字段标注 `⚠`，让参与人知道哪里需要他做判断：
   - 必填区：
     - 原始诉求：保留输入原文，不自行改写。
     - 讨论范围：聚焦从输入内容推断；不涉及必须显式标注，至少写一条。没有明确排除则标注"暂无明确排除"。
     - 已收敛规则：按内容性质选择格式——场景类用 Given/When/Then，架构决策类用"决策/备选/选择/理由"，业务约束类用自然语言 + 影响范围。仅填写输入中有明确结论的内容，没有结论则留空。如果某条规则的 Then 只能写出"完成 XX"、"支持 XX"等笼统描述，说明尚未收敛——不写入已收敛规则，改为放入待收敛模糊点。对任何推断但未明说的规则标注 `⚠`。
     - 待收敛模糊点：除了输入中显提到的未解决问题，还需提取隐含模糊点（模糊动词、未定义术语、隐含异常、角色权限未说明）。每个模糊点必须说明它阻塞了什么。对每条已写出的 Then，检查是否存在隐含的技术或业务细节未明确（如"原路退回"——退回时效？部分退回？）。
4. **创建或更新批次概览**（多需求输入，或单需求但有跨系统依赖时）。按 `schema/batch-overview-cn.md` 格式创建 `BATCH-OVERVIEW.md`，填写：批次元数据、Collection 清单、跨 Collection 关系、全局待收敛清单（带优先级列：P0 / P1 / P2）。单需求输入仅填写适用于单 Collection 批次的区块。
5. **🛑 暂停，等待参与人审阅**。向参与人呈现：
   - 每个起草的 Collection（高亮 `⚠` 标记）。
   - 多需求时：批次概览（清单、关系图、按优先级分组的全局待收敛清单）。
   - 在顶部列出"需要你决策的事项"简表。
   等待参与人解决 `⚠` 标记（接受 / 修正 / 补充模糊点）。状态已经是采集中——此步无需翻状态。

---

## 提示词二：收敛讨论

输入：一个或多个采集中状态的 Collection 文件，以及批次级别的 `BATCH-OVERVIEW.md`（如果批次内有多个 Collection）。
输出：更新后的 Collection 文件。如涉及跨 Collection 的关系变化，同步更新 `BATCH-OVERVIEW.md`。状态可能在末尾由采集中翻为已收敛。

提示词内容：

任务：为参与人产出一份**收敛建议报告**，然后记录参与人的决策。目标是让所有已收敛规则达到可验证的粒度，并清零待收敛模糊点。你提议；参与人决策；你落盘。

步骤：

**阶段一 — 产出收敛建议报告**

1. **粒度检查**。逐条审查已收敛规则中的 Then：
   - 如果 Then 包含"完成 XX"、"支持 XX"、"实现 XX"等笼统描述而没有可验证的具体条件，这条规则**不合格**。在报告中建议将其降级为待收敛模糊点，或拆分为更细的子场景。
   - 合格的 Then 示例："系统返回 200 状态码及订单号"、"购物车中该商品数量 +1，总价重新计算"。
   - 不合格的 Then 示例："完成绑定"、"支持聚合支付"、"订单查询成功"。
2. **为待收敛模糊点提议解决方案**。对每个模糊点，从下列三种中选一：
   - **用 AI 建议解决**：上下文允许合理推断时，给出推断及理由（标注为"AI 建议"）。
   - **升级给参与人**：需要人做决策时，说明阻塞影响，并给出 2-3 个候选选项供参与人选择。
   - **显式延迟**：该点不阻塞任何范围内规则的收敛时，建议延迟并说明理由。
3. **评估优先级**。按 schema/batch-overview-cn.md 中的定义，为每个模糊点分配或复核优先级（P0 / P1 / P2）。
4. **隐式模糊点挖掘**。对每条合格规则再做一次检查：这条规则在实际开发时，开发者是否会追问"具体怎么做"？如果是，补充新的隐式模糊点。常见模式：
   - 涉及外部系统的操作 — 超时怎么处理？失败了怎么降级？
   - 涉及数据变更的操作 — 并发写入怎么办？幂等性怎么保证？
   - 涉及用户输入的操作 — 输入校验规则是什么？非法输入怎么响应？
5. **识别遗漏**。检查已收敛规则是否覆盖了适用范围内的所有主要场景：至少一条正常路径 + 至少一条异常路径。如发现遗漏，提议新增模糊点。
6. **检查一致性**。检查已收敛规则之间是否存在逻辑矛盾。检查讨论范围和已收敛规则是否一致。
7. **🛑 向参与人呈现收敛建议报告**。报告分为三个区块：
   - **AI 建议（接受 / 否决 / 修改）**：每个有 AI 建议解的模糊点。
   - **需要你决策**：每个需要人做选择的模糊点，附 2-3 个候选选项。
   - **建议延迟**：建议延迟处理的模糊点及理由。
   此时不要写入 Collection。等待参与人。

**阶段二 — 记录参与人决策**

8. **落盘决策**。对报告中的每一条，按参与人的决策执行：
   - 接受 AI 建议 → 转为已收敛规则或标记为延迟决策。
   - 否决/修改 AI 建议 → 写入参与人版本。
   - 需要人决策的项 → 记录选中的选项及理由。
   - 延迟项 → 标记为延迟并记录理由。
9. **更新依赖**。如收敛涉及跨 Collection 的关系变化，更新批次级别的 `BATCH-OVERVIEW.md`（跨 Collection 关系图、全局待收敛清单，以及受影响的清单行）。
10. **收敛完成判定**。当且仅当以下全部满足时，可将状态改为已收敛：
    - 粒度检查全部通过（每条 Then 可验证）
    - 待收敛模糊点已全部清零（转化为规则或标记为延迟决策）
    - 收敛检查清单全部勾选
11. **翻状态并同步批次概览**。把每个已收敛 Collection 的状态翻为已收敛，并更新 `BATCH-OVERVIEW.md` 中对应的清单行与全局待收敛清单。

---

## 提示词三：生成技术设计

输入：一个 Collection 文件（状态为已收敛）和目标仓库信息。
输出：一个 Tech Design 文件，状态为草稿，放在 `tech-design/{日期}-{简短主题}/` 下。

提示词内容：

任务：将已收敛的 Collection 转化为 Tech Design 文件，完成从业务规则到技术契约的翻译。

步骤：
0. 检查前提。
   - 先询问设计模式："本次是全新设计（无现有仓库）还是存量设计（基于现有仓库）？" 未确认前不得继续。
   - 若**存量设计**：询问目标仓库地址，未提供前不得继续。
   - 若**全新设计**：目标仓库可选，可留空。
1. 上下文收集（按设计模式分支）：
   - **存量设计**两轮扫描仓库：第一轮读仓库层级的 AGENTS.md / CLAUDE.md / .cursorrules（仓库维护者的权威约定，优先避免后续调查方向跑偏）；第二轮基于约定扫描代码库，填充「仓库上下文」。每个子字段列 1-2 个典型/代表性文件路径即可，不要求穷举。
   - **全新设计**做问题域调查：需求边界、候选技术栈、类似系统参考，填充「问题域上下文」，作为后续技术选型的论证输入。
2. 填充标识。名称与 Collection 主题一致，版本 1.0.0，状态草稿，**设计模式**按 Step 0 结论填写，来源回填 Collection 路径。
3. 填充适用范围。从 Collection 继承，补充技术边界（如"仅覆盖 API 层，不含 UI"）。
4. 场景映射与方案全量调查（合并步骤）。
   为每个 Collection 场景展开完整的技术执行路径，三维度覆盖：
   - 端到端流程调查（每个环节的输入、处理逻辑、输出、失败条件、失败影响、涉及组件）
   - 组件交互调查（调用方、被调用方、协议、数据格式、超时、失败处理、是否有现有实现）
   - 数据流调查（每个数据对象的创建、修改、读取、归档/删除路径及一致性风险）
   存量设计需同时标注每一步是"复用现有 / 修改现有 / 新增"，并指出对应的现有代码位置；全新设计侧重候选技术路径与类似系统参照。
   输出调查结论：覆盖完整度、关键风险、待深入的点。
5. 制定技术策略（基于调查结论）。先确定整体技术路线（架构风格、集成模式、数据管理策略、部署方式），再针对关键选型问题列出备选方案、选择及理由。每个决策必须有至少两个备选。**全新设计**每个决策需要从第一性原理充分论证，不能靠"沿用现有"省略推理。
6. 定义指标（基于调查结论）。为性能、质量、容量三个维度设定可衡量的目标值，给出测量方式。不写"高性能"之类的模糊描述，必须给出具体数字。
7. 定义数据模型（基于调查结论的数据流）。为每个业务实体定义数据结构，写到字段级，标注字段约束和存储方式。存量设计区分新增字段与对现有模型的扩展；全新设计完整定义。关联回 Collection 中的场景。
8. 定义 API 契约（基于调查结论的组件交互）。为每个用户操作定义接口，包括端点、请求/响应字段、前置/后置条件、副作用。存量设计区分新增接口与对现有接口的修改；全新设计完整定义。每个 API 必须关联到 Collection 中的具体场景。
9. 定义外部系统集成（基于调查结论）。列出所有需要交互的外部系统及集成方式。
10. 定义错误处理策略。统一错误码体系和降级方案。
11. 构建需求转换兜底策略。逐条核对覆盖矩阵（每个 Collection 场景是否都有对应的技术路径），定义降级策略（触发条件、降级行为、业务影响、恢复条件），记录遗漏风险。兜底策略必须基于全量调查的结论，不允许跳过调查直接写兜底。
12. 规划实现顺序。按依赖关系排序，先基础后应用，先数据后接口。
13. 跨切面一致性审查。验证 Step 5-12 所有决策之间的内在一致性：
    - 技术策略与指标是否相容（如策略选型能否支撑指标目标）
    - 数据模型是否支撑所有 API 契约的读写需求
    - 场景映射是否完整覆盖所有 Collection 场景（与兜底策略的覆盖矩阵对账）
    - 外部系统集成、错误处理、实现顺序是否彼此一致
    - 如发现不一致，修订相关字段并记录修订理由。
14. 填充变更记录。添加初始版本记录。
15. 回填关联。将 Tech Design 路径回填到 Collection 的关联技术设计字段。

---

## 提示词四：生成实施计划

输入：一个 Tech Design 文件（状态为就绪）。
输出：一个 Implementation 文件，状态为草稿，放在 `implementation/{日期}-{简短主题}/` 下。

提示词内容：

任务：将就绪的 Tech Design 转化为 Implementation 文件，完成从技术契约到代码指令的翻译。

步骤：
0. 检查前提。确认 Tech Design 状态为就绪，目标仓库已确认。
1. 读取输入。读取 Tech Design 文件的全部字段。
2. 填充标识。名称与 Tech Design 一致，版本设为 1.0.0，状态设为草稿，来源回填 Tech Design 路径。
3. 填充版本控制。从目标仓库的 Agent 指令文件和仓库上下文提取分支规范和 PR 要求。
4. 填充适用范围。从 Tech Design 直接映射。
5. 填充接口。从 Tech Design 的 API 契约直接映射，请求/响应结构已定义。
6. 填充业务约束。从 Tech Design 的 API 契约中的前置/后置条件、副作用映射。从场景映射中提取不变式。
7. 填充依赖。从 Tech Design 的外部系统集成和场景映射中的调用链推断前置/后置 Skill 和协作模式。
8. 填充开发约束。从 Tech Design 的错误处理策略和技术约束推断并发策略、事务边界、幂等性、重试降级。
9. 生成变更清单。根据 Tech Design 的实现顺序和数据模型、API 契约，生成具体的变更清单。每个变更项必须包含执行序号、文件路径、结构要求（函数/类型签名），并关联回 Tech Design 的 API 或数据模型。
10. 填充验证。为 Tech Design 的每个场景到技术路径映射生成验证用例，关联到具体的变更项和函数。
11. 填充实现策略。默认测试先行为否，行为驱动为是。
12. 填充变更记录。添加初始版本记录。
13. 回填关联。将 Implementation 路径回填到 Tech Design 的关联实现字段，同时回填到 Collection 的关联实现字段。
