# Tech Design: {主题}

> **状态**: Draft | Ready | Implemented
> **来源 PRD**: [prd/{id}.md](../prd/{id}.md)
> **创建日期**: {YYYY-MM-DD}

---

## 关键决策 (Key Decisions)

> 简单决策（≤2 选项、影响单一 PRD）直接在此记录。
> 复杂决策（>2 选项、影响多个 PRD）考虑升级为独立 ARD。

### KD-1: {决策名}

**背景 (Context)**: 什么情况下需要做这个决策？涉及哪些模块？

**决定 (Decision)**: 最终选择是什么？（一句话）

**备选方案 (Alternatives)**:

| 选项 | 优点 | 缺点 |
|------|------|------|
| {选项 A} | ... | ... |
| {选项 B} | ... | ... |

**理由 (Rationale)**: 为什么选这个而不是其他？

**如何强制 (Enforced by)**: code review / lint / 约定 / 测试

### KD-2: ...

---

## 数据模型 (Data Models)

### {模型名}

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| {field} | {type} | {说明} |

**约束**:
- {field} 不可为空
- {field} 范围: {range}

**关联场景**: PRD 场景 {n}

---

## API 契约 (API Contracts)

### {接口名}

- **端点**: `METHOD /path`
- **用途**: {一句话}
- **来源**: PRD 场景 {n}

**请求**:
```json
{
  "fieldA": "string, required — 说明",
  "fieldB": "number, optional — 说明,默认 X"
}
```

**响应（成功）**:
```json
{
  "code": 0,
  "data": {}
}
```

**响应（失败）**:
```json
{
  "code": {error_code},
  "message": "错误说明"
}
```

**前置条件**: 调用前必须满足的条件
**后置条件**: 调用后系统状态变化
**副作用**: 触发的异步操作或事件

---

## 场景映射 (Scenario Mapping)

| PRD 场景 | 步骤 | 动作 | 组件 | 数据模型 |
|----------|------|------|------|----------|
| {场景 A} | 1 | {动作} | {组件} | {模型} |
|  | 2 | {动作} | {组件} | {模型} |
| {场景 B} | 1 | ... | ... | ... |

---

## 指标 (Metrics)

| 维度 | 指标 | 目标 | 测量方法 |
|------|------|------|----------|
| 性能 | API 延迟 P99 | < {n}ms | APM 监控 |
| 质量 | 可用性 | {n}% | 健康检查 |

---

## 降级策略 (Fallback Strategy)

### 降级场景: {名称}

- **触发条件**: 什么时候触发
- **降级行为**: 降级后做什么
- **业务影响**: 对用户的影响
- **恢复条件**: 什么情况下恢复

---

## 错误处理 (Error Handling)

| 错误码 | 含义 | HTTP 状态 | 处理 |
|--------|------|-----------|------|
| {code} | {含义} | {4xx/5xx} | {retry/degrade/report} |

---

## 变更记录

| 日期 | 变更 |
|------|------|
| {YYYY-MM-DD} | 初始 |
